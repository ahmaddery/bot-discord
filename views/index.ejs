<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Joshua Music Bot - Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        spotify: {
                            green: '#1DB954',
                            black: '#191414',
                            dark: '#121212',
                            gray: '#282828',
                            lightgray: '#b3b3b3'
                        }
                    }
                }
            }
        }
    </script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Circular:wght@300;400;500;600;700&display=swap');
        body {
            font-family: 'Circular', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }
        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }
        .gradient-overlay {
            background: linear-gradient(transparent 0, rgba(0,0,0,.5) 100%);
        }
    </style>
</head>
<body class="bg-black text-white">
    <div class="flex h-screen overflow-hidden">
        <%- include('partials/sidebar', { activePage: 'home' }) %>
        
        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden">
            <%- include('partials/topbar') %>
            
            <!-- Content Area -->
            <main class="flex-1 overflow-y-auto bg-gradient-to-b from-spotify-dark to-black p-8 scrollbar-hide">
                <!-- Hero Section -->
                <div class="relative h-80 rounded-lg overflow-hidden mb-8" style="background: linear-gradient(135deg, #1DB954 0%, #191414 100%);">
                    <div class="absolute inset-0 gradient-overlay"></div>
                    <div class="relative z-10 h-full flex items-end p-8">
                        <img src="<%= botUser.displayAvatarURL({ size: 256 }) %>" alt="Bot Avatar" class="w-58 h-58 rounded-full shadow-2xl mr-6 ring-4 ring-white/20">
                        <div>
                            <p class="text-sm font-bold uppercase tracking-wider mb-2">Music Bot</p>
                            <h1 class="text-7xl font-black mb-4"><%= botUser.username %></h1>
                            <p class="text-spotify-lightgray text-sm">
                                <span class="font-semibold"><%= totalGuilds %></span> servers • 
                                <span class="font-semibold"><%= totalMembers.toLocaleString() %></span> users • 
                                <span class="font-semibold"><%= guilds.filter(g => g.hasQueue).length %></span> active
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Stats Cards -->
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                    <div class="bg-spotify-gray hover:bg-spotify-gray/80 transition rounded-lg p-6 cursor-pointer group">
                        <div class="flex items-center justify-between mb-4">
                            <span class="text-4xl">🏛️</span>
                            <div class="text-right">
                                <p class="text-4xl font-bold text-white"><%= totalGuilds %></p>
                                <p class="text-sm text-spotify-lightgray">Servers</p>
                            </div>
                        </div>
                        <div class="h-1 bg-spotify-green rounded-full transform scale-x-0 group-hover:scale-x-100 transition-transform"></div>
                    </div>

                    <div class="bg-spotify-gray hover:bg-spotify-gray/80 transition rounded-lg p-6 cursor-pointer group">
                        <div class="flex items-center justify-between mb-4">
                            <span class="text-4xl">👥</span>
                            <div class="text-right">
                                <p class="text-4xl font-bold text-white"><%= totalMembers.toLocaleString() %></p>
                                <p class="text-sm text-spotify-lightgray">Total Users</p>
                            </div>
                        </div>
                        <div class="h-1 bg-spotify-green rounded-full transform scale-x-0 group-hover:scale-x-100 transition-transform"></div>
                    </div>

                    <div class="bg-spotify-gray hover:bg-spotify-gray/80 transition rounded-lg p-6 cursor-pointer group">
                        <div class="flex items-center justify-between mb-4">
                            <span class="text-4xl">🎵</span>
                            <div class="text-right">
                                <p class="text-4xl font-bold text-spotify-green"><%= guilds.filter(g => g.hasQueue).length %></p>
                                <p class="text-sm text-spotify-lightgray">Now Playing</p>
                            </div>
                        </div>
                        <div class="h-1 bg-spotify-green rounded-full transform scale-x-0 group-hover:scale-x-100 transition-transform"></div>
                    </div>
                </div>

                <!-- Your Servers -->
                <section class="mb-8">
                    <div class="flex items-center justify-between mb-6">
                        <h2 class="text-2xl font-bold">Your Servers</h2>
                        <a href="#" class="text-sm text-spotify-lightgray hover:text-white transition">Show all</a>
                    </div>
                    
                    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-4">
                        <% guilds.slice(0, 10).forEach(guild => { %>
                        <a href="/server/<%= guild.id %>" class="bg-spotify-gray hover:bg-spotify-gray/60 transition p-4 rounded-lg group">
                            <div class="relative mb-4">
                                <% if (guild.icon) { %>
                                    <img src="<%= guild.icon %>" alt="<%= guild.name %>" class="w-full aspect-square object-cover rounded-md shadow-lg">
                                <% } else { %>
                                    <div class="w-full aspect-square bg-gradient-to-br from-spotify-green to-green-700 rounded-md flex items-center justify-center text-4xl font-bold">
                                        <%= guild.name.charAt(0) %>
                                    </div>
                                <% } %>
                                <% if (guild.hasQueue) { %>
                                    <div class="absolute bottom-2 right-2 bg-spotify-green text-black p-2 rounded-full shadow-lg opacity-0 group-hover:opacity-100 transition">
                                        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                                            <path d="M6.3 2.841A1.5 1.5 0 004 4.11V15.89a1.5 1.5 0 002.3 1.269l9.344-5.89a1.5 1.5 0 000-2.538L6.3 2.84z"/>
                                        </svg>
                                    </div>
                                <% } %>
                            </div>
                            <h3 class="font-semibold truncate mb-1"><%= guild.name %></h3>
                            <p class="text-xs text-spotify-lightgray">
                                <% if (guild.hasQueue) { %>
                                    <span class="text-spotify-green">● Playing</span>
                                <% } else { %>
                                    <%= guild.memberCount %> members
                                <% } %>
                            </p>
                        </a>
                        <% }); %>
                    </div>
                </section>

                <!-- Features -->
                <section class="mb-8">
                    <h2 class="text-2xl font-bold mb-6">Features</h2>
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                        <div class="bg-gradient-to-br from-purple-600 to-blue-600 p-6 rounded-lg">
                            <span class="text-5xl mb-4 block">🎵</span>
                            <h3 class="text-lg font-bold mb-2">SoundCloud</h3>
                            <p class="text-sm text-white/80">Stream music in high quality</p>
                        </div>
                        
                        <div class="bg-gradient-to-br from-green-600 to-teal-600 p-6 rounded-lg">
                            <span class="text-5xl mb-4 block">🤖</span>
                            <h3 class="text-lg font-bold mb-2">AI Autoplay</h3>
                            <p class="text-sm text-white/80">Never stop the music</p>
                        </div>
                        
                        <div class="bg-gradient-to-br from-pink-600 to-rose-600 p-6 rounded-lg">
                            <span class="text-5xl mb-4 block">🔀</span>
                            <h3 class="text-lg font-bold mb-2">Smart Shuffle</h3>
                            <p class="text-sm text-white/80">Intelligent randomization</p>
                        </div>
                        
                        <div class="bg-gradient-to-br from-orange-600 to-red-600 p-6 rounded-lg">
                            <span class="text-5xl mb-4 block">🎚️</span>
                            <h3 class="text-lg font-bold mb-2">Volume Control</h3>
                            <p class="text-sm text-white/80">Perfect sound level</p>
                        </div>
                    </div>
                </section>
            </main>

            <%- include('partials/player') %>
        </div>
    </div>

    <script src="/js/dashboard.js"></script>
</body>
</html>
